import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'capacitor.keycloakjs.authentication',
  appName: 'capacitor-keycloakjs-authentication',
  webDir: 'www'
};

export default config;
